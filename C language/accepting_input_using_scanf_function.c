/*

Fill in the code within the main function to accept and assign the value to the int variable numberOfParticipants using scanf function.   

*/


#include <stdio.h>

int main()
{
    int numberOfParticipants;
    scanf("%d", &numberOfParticipants);
    printf("%d", numberOfParticipants);
    return 0;
}