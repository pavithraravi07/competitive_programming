/*

Fill in the code so that you accept the name of the vegetable passed as input and store it in the char array vegetable.

*/


#include <stdio.h>    

int main() {
    char vegetable[50];
    scanf("%s",vegetable);
    printf("%s",vegetable);
    return 0;
}
 