/*

Fill in the code inside the main function to accept an integer as input, store it in variable weight. Then subtract 10 from the value of the variable weight and print the result as output.

*/


#include <stdio.h>

int main()
{
    int weight;
    scanf("%d", &weight);
    printf("%d", weight-10);
    return 0;
}