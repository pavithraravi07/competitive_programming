/*

Fill in the code inside the main function to print the remainder when the dividend is divided by the divisor.

*/


#include <stdio.h>

int main()
{
    int dividend;
    int divisor;
    scanf("%d%d",&dividend,&divisor);
    printf("%d", dividend%divisor);
    return 0;
}