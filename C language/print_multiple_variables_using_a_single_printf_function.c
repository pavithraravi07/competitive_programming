/*

Fill in the code within the main function to print the values of the three variables firstNumber, secondNumber, thirdNumber separated by a single hyphen (hyphen is nothing but the sign - ).

As an example if the values of the three variables are 10, 20 and 30 the output must be 10-20-30

*/


#include <stdio.h>

int main()
{
    int firstNumber=55,secondNumber=99,thirdNumber=11;
    printf("%d-%d-%d",firstNumber,secondNumber,thirdNumber);
    return 0;
}-